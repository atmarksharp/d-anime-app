{
  "name": "d-anime-app",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-osx": "npm install; electron-packager ./ 'd-anime-app' --version=1.2.2 --platform=darwin --arch=x64 --icon=icon.icns --out=built --overwrite"
  },
  "devDependencies": {
    "electron-packager": "latest",
    "electron-prebuilt": "1.2.2"
  }
}
